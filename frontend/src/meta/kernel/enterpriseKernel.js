import { runKernel } from '../kernel-core/index.js'

export function runEnterpriseKernelBase(input, context = {}) {
  return runKernel(input, context)
}

export function runEnterpriseKernel(input, context = {}) {
  return runKernel(input, context)
}
